#include "validator.h"

// Entity* Validator::get_by_id( GameManager& gm, int in_id ) {

//     return gm.findEntity( in_id );

// }



int Validator::has_monster( Location& l ) {

    return false;

}
int Validator::has_crewman( Location& );
int Validator::has_active_fence( Location& );